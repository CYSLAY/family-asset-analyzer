# 数据安全与升级规则

1. 客户资料以 UUID 为唯一标识，同名客户不会互相覆盖。
2. 输入变化先写入本机 IndexedDB。顾问录入档案由用户点击“保存并同步”后上传；客户自测档案只有在已填写联系人姓名且完成度达到同步阈值后，才会防抖自动上传。
3. 软件升级只能新增字段并为旧数据提供默认值，不清空对象仓库、不批量替换客户文档。
4. 云端同步使用 `updatedAt` 判断版本，不会因为部署新版本而改写客户业务字段。
5. 客户档案页不提供归档；只有用户点击红色“删除”并在弹窗中再次确认，才删除本机和云端记录。云端同时保存仅含客户 UUID、来源与删除时间的删除标记，阻止其他设备或仍打开的客户页面再次上传同一档案；各顾问设备同步时会据此清理本机旧副本。
6. 数据库迁移使用 `create table if not exists` 和增量结构变更；发布前必须在现有数据副本上验证。
7. 正式更新前建议导出加密备份；不得将 `.env.local`、Secret key 或数据库密码提交到仓库。
8. 录入模块状态由当前业务字段实时计算：任一有效字段有内容即显示“已填写”，无内容显示“待确认”；升级不会为改变状态而回写或清空客户资料。
9. Publishable key 会进入浏览器构建产物，它不是数据访问的最终安全边界；数据库表必须启用 RLS、收窄角色 grant，并只通过经过校验的 RPC 暴露必要能力。
10. 当前业务文档在 Supabase PostgreSQL 中不是应用层端到端加密；传输依赖 HTTPS，访问控制依赖数据库权限、RLS、密码/令牌哈希与 RPC 校验。如保存高度敏感或受监管数据，需另行评估字段级加密、Supabase Auth、审计、备份和合规要求。
